name: buddy_client_side
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: "^3.2.3"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  email_validator: 2.1.17
  mask_text_input_formatter: 2.9.0
  fluttertoast: 8.2.4
  shared_preferences: 2.2.2
  intl: 0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/